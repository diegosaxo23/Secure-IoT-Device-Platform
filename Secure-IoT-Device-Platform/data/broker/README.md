# Broker Data Directory

Mosquitto uses this directory for persistent broker state. Runtime files are generated by the broker container and are not committed.

The directory is mounted by Docker Compose. Deleting its contents resets broker persistence but does not delete the platform registry or PKI.
