# Static Assets

`styles.css` contains the dashboard styling for fleet views, device controls, Simulation Lab, registration, and Manufacturing.

The application serves this directory at `/static`. No secret or runtime identity data is embedded in static assets.
